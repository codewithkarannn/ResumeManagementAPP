export interface User {
    token: string;
    role: string;
    // Add other properties if needed
  }

  export interface RegisterUser {
    email: string;
    password: string;
    role: string ;
    // Add other properties if needed
  }